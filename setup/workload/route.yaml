apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: api
  namespace: test
spec:
  parentRefs:
    - name: test
      namespace: test
  hostnames:
    - "toys.apps.demos.kuadrant.io"
  rules:
    - backendRefs:
        - name: toystore
          port: 80